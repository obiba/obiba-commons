module obiba.shiro.web {
  requires com.google.common;
  requires jakarta.servlet;
  requires jakarta.validation;
  requires shiro.core;
  requires spring.beans;
  requires spring.context;
  requires spring.core;
  requires spring.web;
  requires obiba.shiro;
  requires slf4j.api;
}